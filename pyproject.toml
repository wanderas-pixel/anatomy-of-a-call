[project]
name = "transcript-analyzer"
version = "0.1.0"
dependencies = [
    "google-adk>=2.0.0",
    "google-cloud-bigquery",
    "google-cloud-logging",
    "google-cloud-storage",
    "python-multipart",
    "pandas",
    "db-dtypes"
]

[tool.agents-cli]
agent_directory = "app"
is_a2a = true
region = "us"
